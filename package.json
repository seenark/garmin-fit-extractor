{
  "name": "garmin-fit-extractor",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @garmin-fit-extractor/web dev & cargo run -p garmin-fit-extractor-api",
    "build": "bun run --filter '*' build && cargo build --workspace",
    "test": "bun --filter '*' test && cargo test --workspace",
    "test:e2e": "bun run scripts/e2e.ts",
    "check": "bun run --filter '*' check && cargo fmt --check --all && cargo clippy --workspace --all-targets -- -D warnings"
  },
  "packageManager": "bun@1.3.14"
}
